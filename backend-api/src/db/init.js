/**
 * Database schema is managed in Supabase (PostgreSQL).
 * Apply the SQL in `supabase/migrations/001_initial.sql` via the Supabase SQL editor
 * or `supabase db push` if you use the Supabase CLI.
 *
 * Then run: npm run db:seed
 */
console.log(
  "No local DB init: use Supabase. See supabase/migrations/001_initial.sql and README in backend-api."
);
process.exit(0);
